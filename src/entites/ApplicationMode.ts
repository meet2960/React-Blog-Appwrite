export type ApplicationMODE = "DEVLOPEMENT" | "PRODUCTION"

export const applicationModeObj = {
    "DEVLOPEMENT": "DEVLOPEMENT", 
    "PRODUCTION": "PRODUCTION"
}

export enum ApplicationModeEnum{
    "DEVLOPEMENT"="DEVLOPEMENT",
    "PRODUCTION"="PRODUCTION"
}
